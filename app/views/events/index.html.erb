<div class="container">
<% if user_signed_in? %>
  <h1 id="header-font">Welcome, <%= current_user.first_name %> 👋</h1>
 <% end %>
  <div class="d-flex mt-4">
    <div class="calendar-container">
      <div id="calendar">
        my calendar
      </div>
    </div>
    <div class="event-list">
      <div class="date">
        <h3>Wednesday, November 16</h3>
      </div>
      <% @events.each do |event| %>
        <%= link_to event_path(event), class:"d-flex flex-column link" do %>
          <div class="event-container">
            <div class="event-first-container"><img src="https://ichef.bbci.co.uk/news/999/cpsprodpb/15951/production/_117310488_16.jpg" alt=""></div>

            <div class="event-mid-container d-flex flex-column  justify-content-between">

                <div class="d-flex justify-content-between">
                  <h6 class="date-font"><%= event.date %> <%= event.time %></h6>
                  <h6 class="dark-font">Suggested</h6>
                </div>

              <div>
                <h6 class="dark-font"><strong><%= event.name%></strong></h6>
              </div>

              <div>
                <p class="sub-font"><%= event.category%></p>
              </div>

              <div class="d-flex justify-content-between align-items-center" >
                <h6 class="sub-font"><%= event.spots%> attendees</h6>
                <div class="icon d-flex">
                  <i class="fa-solid fa-arrow-up-from-bracket fa-lg"></i>
                  <i class="fa-solid fa-star fa-lg"></i>
                </div>
              </div>

            </div>
          </div>

        <% end %>
        <%# <%= link_to event.name, event_path(event), class:"d-flex flex-column" %>
      <% end %>
    </div>
  </div>
</div>
<%#
<div class="container">
  <h1 class="mb-2">All Events</h1>
  <% @events.each do |event| %>
  <%# <%= link_to event.name, event_path(event), class:"d-flex flex-column" %>
  <%# <% end %>
  <%# </div> %>
<div class="topnav">
  <input type="text" placeholder="Search..">
  <i class="fa-solid fa-magnifying-glass"></i>
</div>
<div class="container">
<h1 class="mb-2">Welcome,User</h1>

<h2> Events from your groups </h2>
<h3> Calender </h3>

<div class="btn-group">
  <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Any type
  </button>
  <div class="dropdown-menu">
    ...
  </div>
</div>
<div class="btn-group">
  <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Any distance
  </button>
  <div class="dropdown-menu">
    ...
  </div>
   <p> Reset filters </p>
   <p> Suggested Events </p>
   <i class="fa-solid fa-square-check"></i>
</div>

<h2> Today </h2>

  <% @events.each do |event| %>
    <%= link_to event.name, event_path(event), class:"d-flex flex-column" %>
  <% end %>
</div>
<div class="container">
<p> No matches found for Tuesday, November 15, 2022 </p>
<h2> Wednesday, November 16 </h2>
</div>

<div class="container">
<p> WED, NOV 16, 6:00 PM </p>
<h2> Wednesday, November 16 </h2>
</div>
